#spring.application.name=03_AccountService

#spring.datasource.username=root
#spring.datasource.password=root123
#spring.datasource.url=jdbc:mysql://localhost:3306/accounts_db
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#logging.file.name=logs/accounts.log
#server.port=8081

spring:
    application:
      name: "account-service"
    datasource:
      username: root
      password: root123
      url: jdbc:mysql://localhost:3306/accounts_db
      driver-class-name: com.mysql.cj.jdbc.Driver
  #  profiles:
   #   active:
   #     - uat
server:
  port: 8081
logging:
  file:
    name: logs/accounts.log 

#Custom properties -Used in application
build:
    version: "1.0.2"
accounts:
    message: "Welcome to MSEDCL Bank Accounts REST API -DEV CHECKING REFRESH"
    contactDetails:
      name: "Jill"
      email: "jill@yopmail.com"
    onCallSupport:
      - 123456890
      - 852149632
